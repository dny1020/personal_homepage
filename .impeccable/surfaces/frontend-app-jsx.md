---
version: 1
slug: "frontend-app-jsx"
primary_target: "frontend/app.jsx"
related_targets: ["frontend/styles.css","frontend/index.html"]
---

Scope: the single page at `/` (danilocloud.me). Visitor mode: Experience.

Audience: technical peers — VoIP, DevOps and infrastructure engineers arriving from GitHub, judging whether the work is real. Job: decide in ~20 seconds whether the depth is genuine, then follow the work outward. Proof on hand: four public repos, five dated roles at one telephony operator, a Raspberry Pi homelab, Credly badges, a generated resume.pdf. Hard constraints, user-confirmed: the buildless stack (single-file JSX + Babel CLI + plain CSS on S3, no bundler, no new runtime dependencies) and the complete, downloadable CV — every role, degree and badge stays present and findable. The photo and the current content order are explicitly in play.

Unresolved: whether the homelab earns its own block or stays inside Achievements.

## Direction contract

THESIS: The page *is* a main distribution frame, not a page about one. It refuses the stacked-section scroll — hero, cards, pills, contact — that the category ships by default.

OWN-WORLD: Equipment-grey enamel ground with painted steel frame members and engraved designation strips. The 25-pair telecom colour code (blue, orange, green, brown, slate) is the only chromatic system, spent exclusively on live jumper wire. Archivo carries structure; JetBrains Mono carries terminal data.

STORY: A peer reads the frame elevation, traces a jumper from a role to the technologies it carried, and leaves toward GitHub convinced the depth is real.

FIRST VIEWPORT: Full-bleed frame elevation, no centred column. A stencilled designation strip across the top carries the name and discipline. Beneath it, labelled horizontal blocks of numbered terminals, packed. One live jumper crosses to the single ACTIVE terminal. The CV download hangs on the frame as its record-copy tag.

FORM: Distribution frame (MDF), candidate 5 of 7 on the ordered grounded list; seed key 736b9bc3.

Raises carried in from declined challengers: named terminal states with printed labels, never wire colour alone (cyclorama); one authored landing, not a fade per section (dossier); density courage — every skill a labelled terminal, not a pill in air (angura poster); the frame owns the whole viewport (shader portal).

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
